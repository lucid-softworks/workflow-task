# Contributing

Install the pinned pnpm version and run the complete local check before opening
a pull request:

```sh
corepack enable
pnpm install
pnpm check
```

Keep changes focused and add tests for changed behavior.
