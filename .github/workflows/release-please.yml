name: Release Please

on:
  push:
    branches: [main]

permissions:
  contents: write
  id-token: write
  issues: write
  pull-requests: write

jobs:
  release-please:
    uses: lucid-softworks/.github/.github/workflows/release-please.yml@v1
    permissions:
      contents: write
      id-token: write
      issues: write
      pull-requests: write
    secrets: inherit
